stages:
  - Build and test
  - Build
  - Deploy
  - Review

variables:
  GIT_DEPTH: 10

.node_cache_template: &node_cache
  cache:
    key:
      files:
        - package-lock.json
    paths:
      - node_modules/
      - .npm/

Frontend unit test:
  stage: Build and test
  image: node:20-alpine3.20
  <<: *node_cache
  script:
    - npm ci --cache .npm --prefer-offline
    - npm run testtofile
  coverage: /All files[^|]*\|[^|]*\s+([\d\.]+)/
  artifacts:
    reports:
      junit: ./junit.xml
      coverage_report:
        coverage_format: cobertura
        path: ./coverage/cobertura-coverage.xml
    expire_in: 1 week
  timeout: 15m
  retry:
    max: 2
    when:
      - runner_system_failure
      - stuck_or_timeout_failure
  interruptible: true
  rules:
    - if: $CI_PIPELINE_SOURCE == "merge_request_event"
    - if: $CI_COMMIT_BRANCH

Frontend lint check:
  stage: Build and test
  image: node:20-alpine3.20
  <<: *node_cache
  needs: []
  script:
    - npm ci --cache .npm --prefer-offline
    - npm run lint
  timeout: 10m
  retry:
    max: 2
    when:
      - runner_system_failure
      - stuck_or_timeout_failure
  interruptible: true
  rules:
    - if: $CI_PIPELINE_SOURCE == "merge_request_event"
    - if: $CI_COMMIT_BRANCH

Frontend image build:
  stage: Build and test
  image:
    name: gcr.io/kaniko-project/executor:debug
    entrypoint: [""]
  script:
    - export NORMALIZED_BRANCH=$(echo $CI_COMMIT_BRANCH | sed -e 's/[^[:alnum:]]//g' | cut -c -29)
    - echo $NORMALIZED_BRANCH
    - echo NORMALIZED_BRANCH=$NORMALIZED_BRANCH > build.env
    - cat build.env
    - >-
      /kaniko/executor
      --context "."
      --dockerfile "./dockerfile"
      --destination docker-registry-emotoagh-service.default/front:$NORMALIZED_BRANCH
      --skip-tls-verify
  artifacts:
    reports:
      dotenv: build.env
    expire_in: 1 day
  timeout: 20m
  retry:
    max: 1
    when:
      - runner_system_failure
  interruptible: true
  rules:
    - if: $CI_COMMIT_BRANCH && $CI_COMMIT_BRANCH != "master" && $CI_COMMIT_BRANCH != "dev"
    - if: $CI_PIPELINE_SOURCE == "merge_request_event"

Prepare frontend review deploy:
  stage: Review
  needs:
    - job: Frontend image build
      artifacts: true
  variables:
    NORMALIZED_BRANCH: f-$NORMALIZED_BRANCH
    FRONTEND_IMAGE: front:$NORMALIZED_BRANCH
    UPSTREAM_BRANCH: $CI_COMMIT_BRANCH
  trigger: 
    project: spectrumagh/webappteam/deployments-WebApp
    branch: dev
  rules:
    - if: $CI_COMMIT_BRANCH && $CI_COMMIT_BRANCH != "master" && $CI_COMMIT_BRANCH != "dev" && $CI_COMMIT_TAG == null

Dev build:
  stage: Build
  needs:
    - Frontend unit test
  image:
    name: gcr.io/kaniko-project/executor:debug
    entrypoint: [""]
  script:
    - >-
      /kaniko/executor
      --context "."
      --dockerfile "./dockerfile"
      --destination docker-registry-emotoagh-service.default/front:dev
      --skip-tls-verify
  timeout: 20m
  retry:
    max: 1
    when:
      - runner_system_failure
  interruptible: true
  rules:
    - if: $CI_COMMIT_BRANCH == "dev"

Dev deploy trigger:
  stage: Deploy
  needs:
    - Dev build
  variables:
    UPSTREAM_BRANCH: $CI_COMMIT_BRANCH
  trigger: 
    strategy: depend
    project: spectrumagh/webappteam/deployments-WebApp
    branch: dev
  rules:
    - if: $CI_COMMIT_BRANCH == "dev"

Production build:
  stage: Build
  needs:
    - Frontend unit test
  image:
    name: gcr.io/kaniko-project/executor:debug
    entrypoint: [""]
  script:
    - >-
      /kaniko/executor
      --context "."
      --dockerfile "./dockerfile"
      --destination docker-registry-emotoagh-service.default/front:$CI_COMMIT_TAG
      --destination docker-registry-emotoagh-service.default/front
      --skip-tls-verify
  timeout: 20m
  retry:
    max: 1
    when:
      - runner_system_failure
  interruptible: false
  rules:
    - if: $CI_COMMIT_TAG

Production deploy trigger:
  stage: Deploy
  needs:
    - Production build
  trigger: 
    strategy: depend
    project: spectrumagh/webappteam/deployments-WebApp
    branch: master
  rules:
    - if: $CI_COMMIT_TAG
